version: '3'
services:
  firestore-emulator:
    image: ghcr.io/ridedott/firestore-emulator
    container_name: firestore-emulator
    environment:
      - FIRESTORE_PROJECT_ID=emulator
      - PORT=8200
    ports:
      - 8200:8200
